🧾 Invoice OCR & Information Extraction System

Hệ thống AI OCR + Information Extraction cho hóa đơn, sử dụng mô hình deep learning (LayoutLMv3) để trích xuất:

🧩 Header (thông tin chung)

📊 Table (bảng hàng hóa)

🧾 Footer (tổng tiền, thuế,...)

Hỗ trợ chạy qua web app để inference nhanh.

🚀 Features

End-to-end pipeline: OCR → Layout → Extraction

Tách riêng model theo từng task:

Header

Table

Footer

Web UI đơn giản để test

Xuất kết quả dạng JSON chuẩn backend

Dễ tích hợp vào hệ thống Matching AI / ERP / RPA

🏗️ Project Structure
invoice_ocr/
├── inference/
│   └── web_app/
│       ├── app.py
│       ├── templates/
│       └── static/
├── requirements.txt
├── requirements-gpu.txt

📦 Model structure (external - NOT in repo):

OCR_MODELS/
└── model_final_layoutlmv3/
    ├── model_final_header/
    │   └── final_model/
    ├── model_final_table/
    │   └── final_model/
    └── model_final_footer/
        └── final_model/
⚙️ Setup Environment
1. Create virtual environment
python -m venv venv
2. Activate

Windows:

venv\Scripts\activate

macOS / Linux:

source venv/bin/activate
3. Install dependencies
👉 CPU
pip install --upgrade pip
pip install -r invoice_ocr/requirements.txt
👉 GPU (recommended for production)
pip install --upgrade pip
pip install -r invoice_ocr/requirements-gpt.txt

⚠️ Ensure CUDA + PyTorch compatibility if using GPU

🤖 Model Setup

⚠️ Models KHÔNG được commit trong repo

Bạn cần:

Copy từ internal storage / server

Hoặc download từ nguồn được cung cấp

Set Environment Variables
$env:MODEL_PATH_HEADER="C:\path\to\model_final_header\final_model"
$env:MODEL_PATH_TABLE="C:\path\to\model_final_table\final_model"
$env:MODEL_PATH_FOOTER="C:\path\to\model_final_footer\final_model"

👉 Linux/macOS:

export MODEL_PATH_HEADER=/path/to/model_final_header/final_model
export MODEL_PATH_TABLE=/path/to/model_final_table/final_model
export MODEL_PATH_FOOTER=/path/to/model_final_footer/final_model
▶️ Run Application
cd invoice_ocr/inference/web_app
python app.py
🌐 Access Web UI
http://localhost:5000/
📤 How to Use

Upload invoice (image/PDF)

Click Extract

Wait for processing

Copy JSON result
